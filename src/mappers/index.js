import mapProperty from "./mapProperty";
import mapVariable from "./mapVariable";
import mapVariableValue from "./mapVariableValue";

const mappers = {
  mapProperty,
  mapVariable,
  mapVariableValue,
};

export default mappers;